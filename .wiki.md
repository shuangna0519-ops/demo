# Project Summary

本项目是一个 AI 应用生成器，用户可以通过自然语言描述生成可运行的单文件 HTML 应用，也可以直接使用无需 AI 额度的内置可玩小游戏模板。生成结果支持隔离预览、代码查看、复制、下载、迭代修改和历史持久化，适用于快速制作游戏、工具和效率类应用。

项目当前提供“内置模板”和“智能体生成”两种模式：

- **内置模板模式**：在浏览器本地匹配并运行数独、贪吃蛇、2048，无需调用 AI 或消耗额度。
- **智能体生成模式**：调用 AI 服务流式生成开放场景的 HTML 应用；支持选择模型，并在渠道故障时自动尝试候选模型。
- 两种模式的结果均可预览、查看代码、复制、下载并保存到 Atoms Cloud 生成历史。

# Project Module Description

- **内置小游戏模板**
  - 提供数独、贪吃蛇和 2048 三个完整可玩模板。
  - 支持键盘及触屏操作、重新开始和对应的游戏规则。
  - 数独支持难度选择、输入校验和完成判断。
  - 贪吃蛇支持方向控制、移动、碰撞检测和计分。
  - 2048 支持方向键、滑动、方块合并、计分及胜负判断。
  - 模板代码在浏览器本地匹配和运行，不依赖 AI 额度。

- **智能体应用生成**
  - 接收自然语言应用描述。
  - 支持选择可用 AI 模型。
  - 调用后端 AI 服务流式生成完整 HTML 应用。
  - 在模型或渠道不可用时按候选顺序自动降级尝试。
  - 区分账户额度不足、渠道不可用等失败原因。

- **双模式主页流程**
  - 默认进入内置模板模式。
  - 支持在内置模板与智能体生成之间切换。
  - 内置模板支持快捷示例和描述匹配。
  - 智能体模式保留开放式应用生成能力。

- **迭代修改**
  - 基于当前应用代码提交修改要求。
  - 保留未被修改的功能并生成新版本。
  - 主要用于智能体生成结果的后续调整。

- **实时预览与交付**
  - 在隔离 iframe 中运行生成的 HTML。
  - 支持预览与代码视图切换。
  - 支持显示实际使用的模型信息。
  - 支持复制完整代码和下载独立 HTML 文件。

- **生成历史**
  - 持久化标题、分类、摘要、代码、模型、版本、来源和修改指令。
  - 内置模板记录标注为“内置模板”。
  - 支持打开记录、还原可玩预览、回填描述、删除及撤销删除。
  - 内置模板和 AI 生成结果使用统一历史流程。

# Directory Tree

```text
app/
├── .mgx/config.yaml                         # 项目平台配置
├── start_app_v2.sh                          # 全栈启动脚本
├── backend/
│   ├── main.py                              # 后端应用入口
│   ├── lambda_handler.py                    # Lambda 适配入口
│   ├── README.md                            # 后端开发说明
│   ├── requirements.txt                     # 后端依赖
│   ├── requirements.default                 # 默认依赖模板
│   ├── alembic.ini                          # 数据库迁移配置
│   ├── alembic/                             # 迁移环境与版本
│   ├── core/                                # 配置、认证、数据库、加密与遥测
│   ├── dependencies/                        # 认证与数据库依赖注入
│   ├── models/                              # ORM 模型
│   │   └── generation_history.py            # 应用生成历史模型
│   ├── schemas/                             # API 数据结构
│   │   └── agent_app.py                     # 生成请求、模型与响应结构
│   ├── routers/                             # API 路由
│   │   ├── agent_app.py                     # 智能体生成与模型相关接口
│   │   └── generation_history.py            # 历史记录接口
│   ├── services/                            # 业务服务与数据访问逻辑
│   │   └── agent_app.py                     # AI 生成、模型降级与错误处理
│   ├── middlewares/                         # 中间件
│   ├── utils/                               # 日志等通用工具
│   ├── data_models/                         # 数据模型示例
│   ├── mock_data/                           # 模拟数据
│   └── skills_docs/                         # 平台能力开发文档
└── frontend/
    ├── .mgx/config.yaml                     # 前端平台配置
    ├── package.json                         # 前端依赖与脚本
    ├── README.md                            # 前端开发说明
    ├── index.html                           # HTML 入口
    ├── vite.config.ts                       # Vite 配置
    ├── tailwind.config.ts                   # Tailwind 配置
    ├── components.json                      # shadcn/ui 配置
    ├── template_config.json                 # 模板配置
    ├── site.config.json                     # 站点配置
    ├── src/
    │   ├── App.tsx                          # 应用根组件
    │   ├── main.tsx                         # 前端入口
    │   ├── App.css                          # 应用样式
    │   ├── index.css                        # 全局样式
    │   ├── api/                             # API 调用
    │   ├── contexts/                        # 认证上下文
    │   ├── hooks/                            # 通用 Hooks
    │   ├── lib/
    │   │   ├── agent.ts                     # 智能体、模型与生成接口封装
    │   │   ├── templates.ts                 # 内置模板目录与匹配逻辑
    │   │   └── ...                          # 认证、配置和通用工具
    │   ├── components/
    │   │   └── agent/
    │   │       ├── HistoryPanel.tsx         # 生成历史面板
    │   │       └── PreviewPane.tsx          # 预览、代码与模型信息面板
    │   ├── pages/
    │   │   ├── Index.tsx                    # 双模式应用生成主页
    │   │   └── ...                          # 认证与博客页面
    │   └── templates/
    │       ├── game2048.html                # 2048 内置可玩模板
    │       ├── snake.html                   # 贪吃蛇内置可玩模板
    │       └── sudoku.html                  # 数独内置可玩模板
    ├── public/                              # favicon 与 robots 文件
    ├── prerender/                           # 博客预渲染与站点地图脚本
    └── tsconfig*.json                       # TypeScript 配置
```

# File Description Inventory

- **智能体后端接口**：`backend/routers/agent_app.py`、`backend/schemas/agent_app.py`、`backend/services/agent_app.py`
- **模型选择与自动降级**：后端上述智能体接口文件，以及 `frontend/src/lib/agent.ts`
- **生成历史数据层**：`backend/models/generation_history.py`、`backend/routers/generation_history.py`、`backend/data_models/generation_history.json`
- **前端主页双模式流程**：`frontend/src/pages/Index.tsx`
- **前端智能体与模型接口**：`frontend/src/lib/agent.ts`
- **内置模板目录与匹配**：`frontend/src/lib/templates.ts`
- **数独模板**：`frontend/src/templates/sudoku.html`
- **贪吃蛇模板**：`frontend/src/templates/snake.html`
- **2048 模板**：`frontend/src/templates/game2048.html`
- **预览、代码交付与模型展示**：`frontend/src/components/agent/PreviewPane.tsx`
- **历史记录管理与模板还原**：`frontend/src/components/agent/HistoryPanel.tsx`
- **通用前端逻辑**：`frontend/src/api/`、`frontend/src/contexts/`、`frontend/src/hooks/`、`frontend/src/lib/`
- **认证与平台基础能力**：`backend/core/`、`backend/dependencies/`、`frontend/src/contexts/AuthContext.tsx`
- **站点与构建配置**：`frontend/vite.config.ts`、`frontend/tailwind.config.ts`、`frontend/src/App.css`、`frontend/src/index.css`
- **博客预渲染**：`frontend/prerender/`

# Technology Stack

- **前端**：React、TypeScript、Vite
- **UI 与样式**：shadcn/ui、Tailwind CSS、Radix UI
- **后端**：Python、平台 API 服务架构
- **AI 能力**：Atoms Cloud AI Hub、流式文本生成、模型候选降级
- **数据持久化**：Atoms Cloud 实体与数据库服务
- **数据库迁移**：Alembic
- **接口与认证**：REST API、平台 Web SDK、认证上下文
- **本地模板运行**：浏览器内置 HTML 模板、模板匹配与 iframe 隔离渲染
- **应用交付**：代码查看、复制、独立 HTML 文件导出
- **质量检查**：ESLint、TypeScript 编译、Python 语法检查、Vite Production Build
- **部署方式**：平台 Web 应用预览与 Publish 发布流程

# Usage

## 前端安装与开发

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
cd app/frontend
npm run lint
npm run build
```

## 后端安装

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Windows：

```bash
.venv\Scripts\activate
```

## 启动全栈应用

```bash
cd app
bash start_app_v2.sh
```

也可以按照 `backend/README.md` 的说明单独启动后端服务。

## 使用内置模板

1. 在主页选择“内置模板”模式。
2. 输入“数独小游戏”“贪吃蛇小游戏”或“2048 小游戏”等描述，或点击对应快捷按钮。
3. 点击生成并试玩。
4. 在预览面板中运行游戏，或切换到代码视图。
5. 复制代码、下载 HTML，或从历史记录重新打开模板。
6. 根据游戏支持的键盘、触屏或滑动操作进行试玩。

内置模板在浏览器本地运行，不需要 AI 额度。

## 使用智能体生成器

1. 在主页切换到“智能体生成”模式。
2. 输入应用或游戏的自然语言描述。
3. 选择可用模型后点击生成。
4. 等待 AI 流式输出完成。
5. 在预览面板中运行应用，查看代码或确认实际使用的模型。
6. 复制代码、下载 HTML，或输入修改要求生成新版本。
7. 在历史面板中打开、回填或删除已有生成记录。

智能体生成受账户 AI 额度和渠道状态限制。额度不足无法通过切换模型绕过；模型或渠道故障时，服务会按候选顺序自动降级，并在全部失败时返回具体原因。

## 平台发布

在 App Viewer 中确认内置模板、智能体生成、预览、迭代和历史记录功能正常后，使用 **Publish** 完成发布。
